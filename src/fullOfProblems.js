// An example source code from the link below.
// https://eslint.org/docs/latest/user-guide/formatters/#fullofproblemsjs

function addOne(i) {
    if (i != NaN) {
        return i ++
    } else {
      return
    }
};
